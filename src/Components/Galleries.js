import styled from "styled-components";

const Galleries = (props) => {
    return (
        <Div>hello weddingslk</Div>
    );
};

const Div = styled.div`
    color: white;
`;

export default Galleries;
